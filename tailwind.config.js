import defaultTheme from 'tailwindcss/defaultTheme';
import forms from '@tailwindcss/forms';

/** @type {import('tailwindcss').Config} */
export default {
    // Mengaktifkan dark mode berbasis class ("dark" di <html>),
    // bukan berbasis preferensi sistem (prefers-color-scheme).
    // Ini WAJIB agar toggle manual (localStorage) bisa mengontrol tema,
    // bukan hanya mengikuti setting OS pengguna.
    darkMode: 'class',

    content: [
        './vendor/laravel/framework/src/Illuminate/Pagination/resources/views/*.blade.php',
        './storage/framework/views/*.php',
        './resources/views/**/*.blade.php',
    ],

    theme: {
        extend: {
            fontFamily: {
                sans: ['Figtree', ...defaultTheme.fontFamily.sans],
            },
        },
    },

    plugins: [forms],
};
